
@{
    var message = TempData["StatusMessage"] as string;
}
@if (!string.IsNullOrEmpty(message)) // neu thong bao khac null
{
    <div class="alert alert-warning" role="alert">
        @message
    </div>
}
