@{

    var product = ViewBag.product as ProductModel;
}
<div class="card bg-warning">
    <h1 class="card-header">
        @product.ProductName
    </h1>
    <div class="card-body">
        <p class="card-text">
            @product.Price
        </p>
    </div>
</div>

